[project]
name = "easy-math-lang"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "duo8097", email = "Imanoob8097@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.3",
]

[project.scripts]
easy-math-lang = "easy_math_lang:main"

[build-system]
requires = ["uv_build>=0.12.16,<0.13.0"]
build-backend = "uv_build"
